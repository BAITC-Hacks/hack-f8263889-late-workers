# Backend origin (no trailing slash). The client calls `${VITE_API_URL}/api/v1/*`
# and `${VITE_API_URL}/health`. Must be listed in the backend's CORS_ORIGINS.
VITE_API_URL=http://localhost:8000

# Build environment tag — one of: development | staging | production
VITE_APP_ENV=development
